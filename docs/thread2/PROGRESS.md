# Thread 2 — PROGRESS journal

- 2026-07-19T12:12:00+04:00 — F3 thesis page and palette verb wiring completed. Added a routed `/thesis` mandate editor backed by the served thesis baseline: shared filter Chips for sector/stage/geography, fixture-derived check-size and risk-appetite presentation, persisted 64/22/14 ranking sliders with value pills, a browser-local/no-server-writes notice, and reset-to-fund-defaults behavior. Extracted the local overlay into `thesisStore.ts`; Radar now consumes the same persisted filters and weights while retaining its exact historical-score/momentum/fit ranking formula, and legacy sector query links hydrate the shared state. Enabled the Thesis top-nav route. Command-palette actions are now explicit: deep dive submits founder/all-three-dimensions through the existing POST client and routes to the accepted run (deterministic mock run in fixture mode), memo navigation remains intact, and sector filtering writes through the thesis store before returning to Radar. Updated the palette assertion and added the tenth Playwright flow proving mock mandate chips, localStorage persistence across reload, and shared Radar state. Final verification: `npm run check` passed (0 errors/0 warnings); `npm run build` passed (143 modules); `npm test -- --list` discovered 10 tests. Full `npm test` started all 10 workers, but the managed macOS sandbox denied Chromium Mach registration (`bootstrap_check_in … MachPortRendezvousServer … Permission denied (1100)`) before any assertion, so browser execution remains for the parent/out-of-sandbox runner.

- 2026-07-19T11:54:06+04:00 — F2 founder-record memo/scoring visuals completed. Added the retrospective recognition story directly to the horizontal trajectory (amber detection, ink-dashed recognition, honest shaded lead/miss interval, and observed/unrecognized/honest-miss headline variants). The signal numeral and “why this score?” affordance now open an accessible additive waterfall with contribution/running-total bars and keyboard/Escape behavior; component hover/focus isolates the mapped timeline evidence type, while axis hover/focus links its memo claims. Memo prose now ends in stable page-wide numbered citation superscripts with the existing evidence popover language and honest minimum-confidence dots. Added a pure-SVG, keyboard-navigable three-column memo→claim→referenced-evidence provenance graph with connected-path dimming and section/claim/timeline navigation. Cached deep-dive summaries/documents now merge back as an optional hand-validated live-capture section (ConfidenceArc, verification, lime-outline provenance, external live evidence links); parseable live evidence is deduplicated onto the historical timeline as distinct lime nodes with a provenance legend. Added normalized frontend run/detail types and real/mock API methods, plus a copied deterministic 3-evidence/2-claim cached run for `VITE_MOCK=1`. Added the required Playwright invariants: waterfall contributions sum within ±0.01 of the displayed score, and every memo prose section renders exactly its fixture `claim_ids` count as numbered citations. Final verification: `npm run check` passed (0 errors, 0 warnings); `npm run build` passed (141 modules); `npm test -- --list` discovered 9 tests; a direct fixture oracle confirmed score/component sum `0.91/0.91` and section citation counts `[2,1,1,1,1,1,1,1]`. Full `npm test` compiled and launched 9 workers, but all were infrastructure-blocked at 0 ms because the managed macOS sandbox denied Chromium Mach registration (`bootstrap_check_in … MachPortRendezvousServer … Permission denied (1100)`), so browser assertions require the parent/out-of-sandbox runner.

- 2026-07-19T11:50:45+04:00 — F1 Radar honest-time-machine and foresight-proof frontend completed. Removed the evidence-maturity `scoreAtMonth` approximation: list-response trajectory points now supply exact/clamped scrub scores, trailing-three-month slope, sparkline history, color, baseline order, and weighted ranking. Added scrub-aware pre-detection dim/caption states with reduced-motion-safe lime boundary flash; API-derived positive/unrecognized/miss chips; and an editorial Proof strip showing the top three observed lead times (Ada 16, Barbara 13, Grace 10) with real trajectories, amber detection markers, dashed recognition markers, shaded intervals, evidence-payload hooks, click-throughs, and the Katherine Johnson honest-miss footnote. Replaced Portfolio with a working `#proof` nav and made Thesis visibly/semantically disabled. Mock candidates now retain D1/F2 recognition and score components plus derived lead time and all 24 compact list trajectory points (cohort audit 9/2/1). Added a seventh Playwright smoke test deriving its cutoff and expected scores directly from mock trajectories. Final `npm run check`: 0 errors/0 warnings. Final `npm run build`: pass (141 modules). `npm test -- --list`: 7 tests discovered. Full `npm test -- tests/smoke.spec.ts` started seven workers, but the managed macOS sandbox killed Chromium before assertions at 0 ms (`bootstrap_check_in … MachPortRendezvousServer … Permission denied (1100)`), so parent/out-of-sandbox execution remains required.


- 2026-07-19: Worktree `live-intel` created off main-thread HEAD (456cd63). Fixtures + thesis.json copied in (data/ is gitignored). SPEC.md authored by Claude (API frozen for parallel T1/T2). Codex launched on T1 (service) and T2 (frontend).
- 2026-07-19T04:59:06+04:00 — T1 service layer completed. Added a standalone Python 3.13 `uv` project with the copied/provenance-guarded memo claim schema, an atomic `vcb-index` Parquet→SQLite/FTS5 builder (all six frozen document types plus denormalized read tables and dangling-evidence verification), every frozen `/api/v1` read endpoint, allow-all CORS, guarded frontend history fallback, and the checked-in OpenAPI snapshot. Fixture-backed SQLite/ASGI/CLI tests pass (`23 passed`); a packaged CLI build verified 12 founder, 12 company, 8 claim, 480 evidence, 27 memo-section, and 19 thesis-term documents; real uvicorn curl smoke checks returned HTTP 200 for health and AGPL search. From `service/`, build with `uv run vcb-index build --data-dir ../data/fixtures --thesis ../config/thesis.json --out ../data/index/vcb.sqlite --verify`; run with `VCB_INDEX=../data/index/vcb.sqlite uv run uvicorn vcb_service.app:app --host 127.0.0.1 --port 8000`; test with `uv run pytest`; refresh the frontend contract with `uv run vcb-openapi`.
- 2026-07-19T05:07:49+04:00 — T2 frontend implemented in `frontend/`: Svelte 5/Vite/TypeScript SPA with the frozen typed `/api/v1` client and preserved OpenAPI snapshot; standalone `VITE_MOCK=1` client backed by copied candidates, trajectories, events, profiles, thesis, and memo fixtures; paper/ink/forest design system with live-only acid lime; responsive Radar (FLIP re-ranking, thesis toggles/weights, visible movement reasons, j/k/Enter navigation, historical evidence scrubber), founder health record (independent three-axis vitals, SVG trajectory, filterable evidence timeline, claim evidence/confidence/verification popovers, contradictions, prominent gaps), static run step-feed shell, and debounced keyboard command palette with verbs and highlighted grouped results. Commands: `npm install`; `VITE_MOCK=1 npm run dev -- --host 127.0.0.1 --port 4173`; `npm run check`; `npm run build`; `VITE_MOCK=1 npm run build`; `npm test`. Type-check passed with 0 errors/0 warnings; real and mock builds passed and produced `frontend/dist`. Four smoke flows were also exercised successfully in the connected browser (12-row boot/all routes, palette search navigation, 9 claim chips + evidence popover + gaps, radar j/k/Enter, 5-step run shell). The formal Playwright command is blocked before assertions by this managed macOS sandbox denying every tested engine's Mach service registration (`bootstrap_check_in ... Permission denied`): final runner output is 0 passed, 4 infrastructure failures at 0 ms; test traces are generated under ignored `frontend/test-results/`. No screenshots captured.

- 2026-07-19 (Claude verification, T1+T2 gates): PASS.
  - service: 23/23 pytest; index --verify ok (12 founders / 480 evidence / 8 claims / 27 memo sections / 19 thesis terms); all /api/v1 read endpoints 200 with ~1ms latency; evidence after/before filtering verified server-side; claim resolution verified (bare claim ids — fixture ids globally unique).
  - frontend: 4/4 Playwright (run outside Codex sandbox — its failure was env-only); real-API build served by the service at /; verified in browser: radar + time-scrubber ribbon + thesis controls, palette live search with grouped/<mark> results and click-through navigation, founder record with never-averaged axes, evidence timeline, claim popover (Verified · 90% confidence · linked evidence refs), gaps rendered.
  - Minor follow-up: Enter-to-navigate in the palette didn't fire under the embedded browser pane's synthetic keys (Playwright keyboard test passes; likely pane artifact — re-verify in real Chrome; fix in T3 pass if real).
  - T3 (code-mode deep-dive agent per amendment) handed to Codex.

- 2026-07-19T05:42:00+04:00 — T3 live deep-dive completed per the amendment. Added the Pydantic-AI/OpenRouter code-mode agent with one `run_python` tool and typed `finalize_run`, a resource-limited Monty sandbox, read-only SQLite, cached/backed-off GitHub/HN/optional SerpAPI providers, recorded fixtures, middleware-enforced tool/GitHub/SQL/output limits, untrusted-output provenance, and boundary-enforced evidence references. Added bounded background execution (2 concurrent/30 rolling-day defaults, env-tunable), durable exact-shape `data/deepdives/<run_id>.json` documents, live audit/SSE streaming, 300 ms cached replay, run retrieval/listing, and OpenAPI contracts. Wired the candidate action to POST the run and the run page to EventSource with live claim cards; `VITE_MOCK=1` retains a deterministic fixture feed. Added no-network FunctionModel tests proving unknown evidence rejection, actual INSERT/UPDATE/DROP denial with the index unchanged, 1:1 audit-step persistence, replay SSE, POST replay, and concurrency/daily admission limits. Final verification: `30 passed`; Svelte check `0 errors, 0 warnings`; production Vite build passed; offline mock persisted a 16-step/3-evidence/2-claim run whose claim refs all resolve. Live: from `service/`, run `VCB_INDEX=../data/index/vcb.sqlite OPENROUTER_KEY=... uv run uvicorn vcb_service.app:app --host 127.0.0.1 --port 8000`, then POST `/api/v1/deepdive`. Mock/offline: `uv run python -m vcb_service.agent.demo --mock ada-lovelace-fixture`; replay its returned run ID at `/runs/<run_id>` or `/api/v1/deepdive/runs/<run_id>/stream`.

- 2026-07-19 (Claude verification, T3 gate): PASS with one fix applied.
  - 30/30 pytest incl. trust properties: draft_claim rejects unknown evidence_id; SQL mutations denied (verified live too — agent's ALTER attempt blocked mid-run); audit steps persist 1:1; replay streams cached steps.
  - Mock demo run: 16 steps / 3 evidence / 2 claims, all refs resolve, provenance "live".
  - LIVE run #1 (real OpenRouter, claude-sonnet-4.5): agent recovered from fetch failures and explored schema, BUT burned its budget guessing add_evidence kwargs → 0 claims. Root cause: system prompt listed function names without signatures. FIX (Claude): documented exact signatures + no-variable-persistence in runner.py system prompt.
  - LIVE run #2 after fix: clean 50-step arc (plan → sql exploration → fetch attempts → web/hn → 5 claims → 5 honest gaps → dimension note → done, outcome OK). All 5 claims cite fx-* evidence verified present in the index; gaps honest (revenue/team/funding not disclosed). Frontend /runs view renders the replay with step feed, sandbox code, trust-contract panel, lime live styling.
  - Raw run doc's top-level `outcome` stays null (API derives it from the done step) — cosmetic, noted for T4 cleanup.

- 2026-07-19T08:43:44+04:00 — T2.5 round 1 UI elevation completed in `frontend/`. Added the reusable `Chip` (filter/nav/event/status), `CircularIconButton`, `MiniArea`, `MiniBars`, and `ConfidenceArc` primitives with hand-rolled SVG, shared paper/ink/forest tokens, 26px/16px layered radii, double shadows, tabular numerals, and reduced-motion handling. Rebuilt the founder record with a 64px identity header, source/company/login chips, scroll-tracked Overview/Timeline/Memo/Claims/Runs navigation, and a single inline signal/momentum/never-averaged-axis/first-detection vitals strip. Replaced the flat evidence list and separate trajectory chart with the horizontal evidence trajectory: real trajectory-colored SVG spine segments, month/year markers, lime detection halo, count-sized monthly evidence nodes, alternating rounded branches and type-specific cards, real client-side commit/star/release metrics and micro-series with event-count fallback, mixed-event chips, FLIP filtering, drag/arrow scrolling, keyboard month stepping/card expansion, and viewport range echo. Claim popovers, confidence/status provenance, gaps, deep-dive creation/run navigation, real API mode, and mock mode remain wired. Added a fifth Playwright test that derives expected distinct-month counts from `events.json` and verifies commit filtering collapses nonmatching month nodes. Verification: `npm run check` passed with 0 errors/0 warnings; real and `VITE_MOCK=1` `npm run build` passed; `npm test -- --list` discovers all 5 tests. Formal `npm test` reached 5 workers but the managed macOS sandbox denied Chromium Mach registration (`bootstrap_check_in ... Permission denied`) before any assertion, so all 5 are infrastructure-blocked at 0 ms and require parent/out-of-sandbox execution.

- 2026-07-19 (Claude, R1 gate): PASS after one bounce. Codex bounce task hung (60+ min silent) — cancelled; Claude applied the 5 fixes directly (chip dedup, MiniBars geometry, month-aggregate copy, normalized spine gradient + open-at-present, breadcrumb chip collapse via legacy 30px button rule). DOM-verified gradient range rgb(191,201,194)→rgb(28,81,62); 5/5 Playwright. R2 (radar ribbon + rows, run-view trace) handed to Codex.

- 2026-07-19T10:25:38+04:00 — T2.5 round 2 UI elevation completed in `frontend/`. Rebuilt the radar scrubber as an ink workspace ribbon with 24 month ticks, year markers, real cross-candidate monthly evidence-event badges, a lime range knob, attached future-evidence disclosure, and native arrow-key month stepping. Radar rows now lazily cache/throttle `/candidates/{login}` detail reads (three concurrent, viewport-triggered) for real 90px trajectory `MiniArea` charts and memo-ready independent-axis `MiniBars`; momentum/evidence use shared terse chips, scores use candidate-relative muted→forest interpolation, and hover/keyboard selection lift while FLIP ranking, j/k, and movement explanations remain. Thesis toggles now use shared filter `Chip`s and sliders expose value pills without changing the ranking formula. Rebuilt the run feed as a typed-color vertical trace with nodes/spine, payload branch elbows/cards, three-line expandable mono reason sandboxes, `ConfidenceArc` + verification-chip claim cards, and a live newest-node lime pulse with a reduced-motion ring; SSE/replay and the trust contract are unchanged. Extended `CircularIconButton` with a functional chevron. Added a sixth Playwright test for 24 ribbon ticks plus pointer/keyboard cutoff changes. Verification: `npm run check` passed (0 errors, 0 warnings); `npm run build` passed (137 modules); `npm test -- --list` found 6 tests. Full `npm test` launched 6 workers but the managed macOS sandbox blocked Chromium before assertions (`bootstrap_check_in ... Permission denied`), producing 0 passed / 6 infrastructure failures at 0 ms; out-of-sandbox execution is still required.

- 2026-07-19 (Claude, R2 gate): PASS after two direct fixes. Codex R2 delivered ribbon/rows/trace; Claude fixed (1) radar auto-scroll on load (grid focus without preventScroll pushed the ribbon off-screen — also root cause of odd full-page captures), (2) row sparklines stuck at 1 point (Svelte hidden-dependency: template called trajectoryValues(candidate) which read `details` internally, never re-evaluating after lazy fetch; now passes `detail` explicitly). DOM-verified 26-segment paths; 6/6 Playwright; ribbon/badges/knob and run-trace nodes/arcs verified in captures.

- 2026-07-19T11:18:02+04:00 — D1 data/backend round completed. Regenerated the deterministic synthetic JSON/Parquet fixture corpus with nullable conventional-signal recognition, an exact 9 recognized-after / 2 detected-but-unrecognized / 1 inbound-miss story (6–16 month positive lead times), and 4–6 evidence-type-derived score contributions summing to every current score. Extended the immutable SQLite candidate read model with additive recognition/component JSON columns; `--verify` now rejects component-sum drift and prints the lead-time distribution. Extended list responses with recognition, derived lead time, and all 24 compact trajectory points, and detail responses with recognition plus score components; partial real-data rows may omit both new upstream fields and are normalized safely. Added byte-determinism, cohort, sum, index-failure, optional-upstream, and endpoint-shape tests; refreshed the OpenAPI snapshot and documented the fields as OPTIONAL in the merge contract. Final no-network suite: `35 passed in 0.30s`. Packaged index verification: 12 founders / 12 companies / 8 claims / 480 evidence / 27 memo sections / 19 thesis terms, lead-time distribution 9 / 2 / 1, `verification: ok`. Rebuilt `data/index/vcb.sqlite`; real uvicorn curl smoke returned 200 and showed list recognition + 16-month lead + 24 trajectory points, while detail showed four components summing to 0.91. Two generator runs had identical fixture bytes (candidate JSON SHA1 `494fcbd3413ec9cde9d15a562ea7f7540c527015`, Parquet SHA1 `6e4130d9e6b6463eabc44eb1bc62cd357e2e17af`).

- 2026-07-19 (Claude, D1 gate): PASS. 35/35 pytest; index --verify prints lead-time distribution 9/2/1, verification ok; live endpoint smoke: list shows lead_time_months=16 + 24 trajectory pts for ada, detail shows 4 components summing 0.91. Cohort hand-audited in candidates.json (miss = inbound Katherine Johnson — apt). F1+F2 launched in parallel after platform classifier outage cleared.

- 2026-07-19 (Claude, F1+F2 gate): PASS after three direct fixes. Codex delivered foresight strip/honest scrub (F1) and waterfall/citations/provenance-graph/merge-back (F2); Claude fixed (1) page-killing RangeError (monthLabel on empty month pre-load — radar rendered 0 rows in BOTH modes; guarded date formatting + trajectory-optional fallback), (2) proof-strip evidence hooks stuck on placeholder (hidden-dependency again — evidence passed explicitly), (3) merge-back curation: only latest OK run merges (was: all 4 incl. a failed run; filter chips polluted to ~25 junk types, observation count silently blended) — now 9 backtest chips + single lime Live-capture chip, honest count split, "N earlier captures retained" note. 9/9 Playwright; captures verified: proof strip with real hooks + 16/13/10-mo cards + honest-miss footnote, waterfall 0→91 additive, provenance graph 3-column, curated capture section.

- 2026-07-19 (Claude, F3 gate): PASS clean — no bounce needed. 10/10 Playwright (run out-of-sandbox), build green, /thesis verified in browser (mandate chips, weight pills, local-config banner, shared store driving radar), palette "Deep dive on…" verb present and wired to POST. All three navs live. Thread-2 demo stack feature-complete: radar+scrubber+proof, record (timeline/waterfall/citations/provenance-graph/curated capture), runs trace, palette, thesis.

- 2026-07-19T15:15:20+04:00 — I1 real-data integration completed. Added `vcb-integrate build` with read-only source guards, percentile normalization, highest-confidence YC recognition joins, attribution-derived additive waterfalls, honest repo/monthly/detector/YC evidence, byte-verbatim memo copies, minimal profiles, staged publication, and verification summaries. Real gates: 100 candidates; 100/100 recognition; 99/100 populated score components; 4,090 evidence rows; 67/67 memo refs resolved across all 6 source memos; `vcb-index --verify` green; 39/39 pytest; real curl smokes green; frontend production requests now cover 100 candidates/500 evidence rows and the 66-month scrubber keeps every tick while thinning labels. thread2 merged overnight/poc@373ec60 and reads data/{scores,labels,memos,events} read-only via vcb-integrate; contact via journal before reshaping those outputs.

- 2026-07-19 (Claude, I1 gate): PASS — full real-data integration verified.
  - vcb-integrate --verify: 100 candidates, recognition join 100/100, components 99/100 (one honest [] for all-zero attributions), 67/67 memo refs resolved across SIX real memos, 4090 evidence rows (2931 activity months, 1147 repo creations, 6 detector crossings, 6 YC listings).
  - Lead-time distribution audited from the index: min 12 / median 14 / max 48 months, zero non-positive. Faithful to the main thread's detector_crossings; the 12–15 clustering mirrors their 12-month-radar design — phrase demo claims accordingly.
  - 39/39 pytest; 10/10 Playwright (mock suite untouched); server on the real index; browser gate: real Proof strip (Savio Martin 48mo, Sashank Gondala/Chris Lo 23mo), 123vivekr record end-to-end (recognition band "13 months ahead", real GH Archive timeline, real memo, provenance graph with real repos, honest gaps incl. "no traction metrics disclosed").
  - Demo framing note: the entire cohort is YC-validated by construction → "not-yet-recognized: 0, miss: 0" is correct for this export; describe as "100-founder YC validation cohort".
